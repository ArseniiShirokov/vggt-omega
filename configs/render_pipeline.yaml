# Per-run scene and chunk interval (change these between runs).
scene:
  log_id: 00a6ffc1-6ce9-3bc3-a060-6006e9893a1a
  frame_start: 0
  frame_end: 9

# Standard pipeline parameters.
data_root: argoverse2
checkpoint: /home/jovyan/users/shirokov/thirdparty/vggt-omega/weights/vggt_omega_1b_512.pt
output_dir: outputs/render_test

image_resolution: 512
target_fps: 10.0
device: cuda
crop_bottom: 205

crop_cache_dir: null
sky_mask_cache_dir: outputs/sky_masks
sam2_cache_dir: outputs/render_test/sam2_cache

conf_percentile: 0.0
align_metric: true
save_comparison: true
comparison_fps: null

dynamic_filter: true
dynamic_filter_mode: combined
scale_error_threshold: 0.08
min_box_displacement_m: 0.2
box_filter_expand_ratio: 1.15
sam2_model_id: facebook/sam2.1-hiera-large
debug_dynamic_filter: true
